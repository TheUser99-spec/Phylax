<section class="hero-shell" id="top">
  <nav class="site-nav" aria-label="Primary navigation">
    <a class="brand-mark" href="#top" aria-label="Phylax home">
      <img src="/logo.jpg" alt="" class="brand-logo" width="28" height="28" />
      <span>Phylax</span>
    </a>
    <div class="nav-links">
      <a href="#top">Home</a>
      <a href="/docs">Docs</a>
      <a href="/faq">FAQ</a>
      <a href="/development-path">Development Path</a>
      <a href="https://github.com/TheUser99-spec/Phylax" target="_blank" rel="noreferrer">GitHub</a>
      <a href="https://x.com/Phylaxdev" target="_blank" rel="noreferrer">X</a>
    </div>
  </nav>

  <div class="site-shell hero-layout">
    <div class="hero-copy">
      <p class="eyebrow" data-hero-reveal>Windows security layer for AI coding agents</p>
      <h1 data-hero-reveal>Phylax stops AI agents before they touch your private files.</h1>
      <p class="hero-body" data-hero-reveal>
        Real OS-level protection. The kernel returns ACCESS_DENIED, the agent never sees a single byte.
      </p>
      <p class="hero-superpowers" data-hero-reveal>
        100% local &nbsp;&middot;&nbsp; No accounts &nbsp;&middot;&nbsp; No cloud &nbsp;&middot;&nbsp; No telemetry
      </p>
      <div class="btn-row" data-hero-reveal>
        <a class="button button-primary" href="#install">Get Started</a>
        <a class="button button-secondary" href="https://github.com/TheUser99-spec/Phylax" target="_blank" rel="noreferrer">View on GitHub</a>
      </div>
    </div>

    <div class="hero-stage" data-hero-reveal>
      <div class="hero-terminal">
        <div class="ht-bar">
          <span class="ht-dot"></span><span class="ht-dot"></span><span class="ht-dot"></span>
          <span class="ht-title">phylax daemon</span>
        </div>
        <div class="ht-body">
          <div class="ht-line"><span class="ht-dim">08:16:08</span> <span class="ht-tag ht-tag-agent">AGENT</span> opencode.exe detected</div>
          <div class="ht-line"><span class="ht-dim">08:16:08</span> <span class="ht-tag ht-tag-try">TRY</span> opencode.exe → .env</div>
          <div class="ht-line"><span class="ht-dim">08:16:08</span> <span class="ht-tag ht-tag-match">MATCH</span> deny bucket → .env</div>
          <div class="ht-line ht-line-block"><span class="ht-dim">08:16:08</span> <span class="ht-tag ht-tag-deny">BLOCK</span> ACCESS_DENIED</div>
          <div class="ht-line"><span class="ht-dim">08:16:09</span> <span class="ht-tag ht-tag-audit">AUDIT</span> stored in local SQLite</div>
        </div>
        <div class="ht-foot">
          <img src="/image1.png" alt="" width="16" height="16" />
          <span>Phylax</span>
          <span class="ht-live">● LIVE</span>
        </div>
      </div>
    </div>
  </div>

  <div class="scroll-cue" aria-hidden="true">See how it works</div>
</section>

<script>
  import { gsap } from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';

  gsap.registerPlugin(ScrollTrigger);

  if (!window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
    gsap.fromTo(
      '[data-hero-reveal]',
      { opacity: 0, y: 32 },
      {
        opacity: 1,
        y: 0,
        duration: 0.8,
        ease: 'power3.out',
        stagger: 0.08,
        delay: 0.1,
      }
    );

    gsap.to('.hero-stage', {
      scrollTrigger: {
        trigger: '.hero-shell',
        start: 'top top',
        end: 'bottom top',
        scrub: true,
      },
      y: -24,
      transformOrigin: '50% 50%',
      ease: 'none',
    });
  }
</script>
